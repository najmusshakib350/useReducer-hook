import bookdata from "./../assets/booksdata";

const Bookreducer=(currentstate={bookdata},action)=>{
    console.log("Hello i am still now into the Bookreducer function");
    return currentstate;
}

export default Bookreducer;

